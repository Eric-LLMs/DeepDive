# DeepDive — runtime dependencies.
# Mirrors [project].dependencies in pyproject.toml. Prefer the editable install:
#   pip install -e .
# This file is the pip-only alternative (no build backend needed).

# Web / API
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0
python-multipart>=0.0.9
PyJWT>=2.8

# Data
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pgvector>=0.2.5
pydantic>=2.6
pydantic-settings>=2.2

# Cache / queue
redis>=5.0
arq>=0.26

# LLM
openai>=1.30
litellm>=1.40
tenacity>=8.2

# MCP
fastmcp>=0.2

# CJK keyword channel (jieba segmentation; lazy-imported on first CJK query/ingest)
jieba>=0.42

# Document parsing for the query repository: PyMuPDF (PDF text + table detection →
# vision transcription), python-docx (Word .docx). Both lazy-imported in
# core/infrastructure/{pdf,ingest}.py so the API stays light until a file is ingested.
PyMuPDF>=1.24
python-docx>=1.1

# gRPC (internal retrieval service)
grpcio>=1.62
grpcio-tools>=1.62

# Misc
httpx>=0.27
jsonschema>=4.21
