"""Agent execution engine: kernel, turn loop, context, decisions, runtime.

Docstring-only: submodules are imported by concrete path (core convention); no eager
re-exports here, which is what keeps the tools.subagent -> engine.loop chain cycle-safe.
"""
